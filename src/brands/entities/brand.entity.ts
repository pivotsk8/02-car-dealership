//El entity es como una abstracion de como seria una coleccion en la DB o tabla
//normalmente las ententies son de las rares clases que no tiene la extencion BrandEntenty(x), Brand
export class Brand {
id:string;
name:string;

createdAt:number;
uodatedAt?:number;
  updatedAt: number;
}
